# 한국 사용자용 종합 커머스 디자인 가이드

> 참고 사이트: [SSG.COM](https://www.ssg.com/)  
> 확인일: 2026-08-29  
> 목적: SSG.COM의 정보 구조와 사용성 패턴을 참고하되, 상표·로고·문구·이미지·고유한 시각 표현은 복제하지 않는 독립적인 커머스 UI 설계

## 1. 디자인 방향

### 핵심 인상

- **통합 탐색:** 넓은 통합검색과 카테고리 진입점을 헤더의 핵심으로 둔다.
- **콘텐츠 층위:** 공지·프로모션·상품 영역을 분리하고, 상품 정보는 일정한 카드 규칙으로 정돈한다.
- **가격 위계:** 정상가·할인율·판매가가 있을 때 시각적 우선순위를 명확히 한다.
- **한국형 커머스 문법:** 익숙한 용어와 원화 표기를 사용하고, 사용자 메뉴와 장바구니 위치를 일관되게 유지한다.
- **브랜드 독립성:** 참고 사이트의 로고, 고유 색상 조합, 배너 구성, 카피, 아이콘 또는 상품 이미지를 그대로 사용하지 않는다.

### 사용자 경험 원칙

1. 사용자는 어느 화면에서도 1회 행동으로 검색을 시작할 수 있어야 한다.
2. 가격과 배송 조건은 상품명보다 찾기 쉬워야 하며 숨겨진 조건을 만들지 않는다.
3. 색상만으로 할인, 품절, 선택, 오류 상태를 구분하지 않는다.
4. 모바일에서는 구매 행동을, 데스크톱에서는 비교와 탐색을 더 쉽게 만든다.
5. 이벤트의 시각적 강도보다 콘텐츠 가독성과 접근성을 우선한다.

## 2. 정보 구조

```text
홈
├─ 통합검색
├─ 전체 카테고리
├─ 기획전·이벤트
├─ 상품 목록·상세
├─ 장바구니·주문
└─ 마이페이지·고객지원
```

헤더는 `브랜드 영역 → 통합검색 → 카테고리·주요 탐색 → 로그인·회원가입·마이페이지·장바구니` 순서의 2단 구조를 기본으로 한다. 로고와 브랜드명은 실제 구현에서 제외할 수 있다. 홈 본문은 `공지·프로모션(데이터가 있을 때) → 주목 상품 → 상품 목록` 순서를 기본으로 한다. 우리 쇼핑몰에 없는 검색·프로모션·브랜드 데이터는 화면에 임의로 채우지 않는다.

## 3. 레이아웃

### 데스크톱

- 최대 콘텐츠 너비: `1200px`
- 좌우 여백: 최소 `24px`
- 12열 그리드, 열 간격 `24px`
- 헤더: 유틸리티 메뉴, 통합검색, 사용자 메뉴, 주요 내비게이션의 2단 구조
- 검색창: 헤더 중앙의 가장 넓은 영역에 배치하고 검색 버튼을 입력창과 결합
- 상품 목록: 화면 너비에 따라 4열 또는 5열
- 프로모션 배너: 이미지 위 텍스트를 합성하기보다 HTML 텍스트를 별도 레이어로 배치

### 모바일

- 기준 너비: `360px` 이상
- 좌우 여백: `16px`
- 상품 목록: 기본 2열, 카드 간격 `12px`
- 상단: 브랜드 영역, 검색, 사용자 메뉴, 장바구니를 간결하게 유지
- 하단 고정 내비게이션: 홈, 카테고리, 검색, 찜, 마이
- 상품 상세의 주요 구매 버튼은 안전 영역을 고려한 하단 고정 영역 사용

### 반응형 기준

- `0–767px`: 모바일
- `768–1023px`: 태블릿
- `1024–1279px`: 소형 데스크톱
- `1280px 이상`: 와이드 데스크톱

중단점은 특정 기기명이 아니라 콘텐츠가 깨지는 지점을 기준으로 최종 조정한다.

## 4. 디자인 토큰

### 색상

SSG.COM의 흰색 중심 표면, 짙은 중립 텍스트, 얇은 회색 경계선과 강한 강조색이라는 대비 구조만 참고한다. 식별 가능한 브랜드 색을 그대로 복제하지 않으며, 아래 값은 우리 서비스의 독립적인 팔레트다.

| 역할 | 토큰 | 값 | 사용 규칙 |
|---|---|---:|---|
| 주요 행동 | `--color-primary-700` | `#4F7F12` | 흰색 텍스트가 있는 주요 버튼 |
| 주요 강조 | `--color-primary-500` | `#8FBC24` | 배지, 장식, 선택 배경. 작은 흰색 글자 금지 |
| 연한 강조 | `--color-primary-100` | `#EEF7D8` | 카테고리·혜택 배경 |
| 기본 텍스트 | `--color-neutral-950` | `#171717` | 제목과 본문 |
| 보조 텍스트 | `--color-neutral-700` | `#4F4F4F` | 설명, 메타 정보 |
| 비활성 텍스트 | `--color-neutral-500` | `#737373` | 큰 텍스트 또는 보조 정보에 한정 |
| 경계선 | `--color-neutral-200` | `#E5E5E5` | 카드, 구분선, 입력창 |
| 표면 | `--color-surface` | `#FFFFFF` | 기본 배경 |
| 보조 표면 | `--color-surface-muted` | `#F7F7F5` | 섹션 구분 |
| 할인·오류 | `--color-danger-700` | `#B42318` | 할인율, 오류. 아이콘·문구 병기 |
| 정보·포커스 | `--color-focus` | `#005FCC` | 키보드 포커스 링 |

- 일반 텍스트 명암비는 WCAG AA 기준 `4.5:1` 이상, 큰 텍스트와 UI 경계는 `3:1` 이상을 확보한다.
- 밝은 라임색은 흰색과 조합 시 명암비가 부족할 수 있으므로 배경 장식이나 짙은 글자와만 사용한다.
- 할인과 품절은 색상 외에 `할인`, `품절` 같은 텍스트 라벨을 반드시 표시한다.
- 다크 모드를 제공한다면 색을 단순 반전하지 말고 별도 토큰 세트를 검증한다.

### 글꼴

```css
font-family: Pretendard, "Noto Sans KR", -apple-system, BlinkMacSystemFont,
  "Segoe UI", Roboto, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
```

- 기본 본문: `16px / 1.6`, `font-weight: 400`
- 보조 정보: 최소 `13px / 1.45`
- 섹션 제목: `24–32px / 1.3`, `font-weight: 700`
- 상품명: `14–16px / 1.45`, 최대 2줄 말줄임
- 가격: `18–24px`, 숫자와 `원` 사이를 띄우지 않음
- 본문 자간: `-0.01em` 이내. 과도한 음수 자간을 피한다.
- 한국어 문장은 `word-break: keep-all; overflow-wrap: break-word;`를 기본으로 한다.

폰트는 시스템 폰트를 우선한다. Pretendard 또는 Noto Sans KR을 포함할 경우 공식 배포처의 라이선스를 확인하고, 프로젝트 내부에서 자체 호스팅하며 WOFF2만 제공한다. 사용자 입력 문자열로 폰트 URL이나 CSS를 생성하지 않는다.

### 간격·모서리·그림자

- 간격 단위: `4, 8, 12, 16, 24, 32, 48, 64px`
- 버튼과 입력창 높이: 데스크톱 `44px`, 모바일 `48px` 권장
- 터치 영역: 최소 `44 × 44px`
- 카드 모서리: `12px`, 입력창·버튼: `8px`
- 그림자: `0 4px 16px rgb(0 0 0 / 8%)`; 장식보다 계층 구분에만 사용

## 5. 핵심 컴포넌트

### 통합검색 (참고 구조)

- 데스크톱 헤더 중앙, 모바일 상단의 가장 넓은 영역에 배치한다. SSG.COM처럼 검색을 헤더의 주요 행동으로 보이게 한다.
- 플레이스홀더 예시: `상품, 브랜드, 성분을 검색해 보세요`.
- 검색어 자동완성은 키보드 방향키와 `Enter`, `Esc`를 지원한다.
- 최근 검색어는 사용자의 기기에만 저장하거나 저장 동의를 받고, 전체 삭제 기능을 제공한다.
- 검색어는 화면에 출력하기 전에 이스케이프하고, URL 파라미터와 분석 로그에 민감정보가 남지 않도록 한다.

### 내비게이션·카테고리 (참고 구조)

- 1차 메뉴는 핵심 항목만 노출하고 나머지는 `전체 카테고리`에 둔다. 현재 상품 데이터가 네 분류뿐이면 네 분류만 표시한다.
- 데스크톱 메가 메뉴는 포인터 이탈 지연과 키보드 탐색을 함께 지원한다.
- 현재 위치는 색상, 굵기, 밑줄 또는 `aria-current`를 함께 사용해 표시한다.

### 프로모션 배너

- 한 화면에 핵심 메시지는 하나만 둔다.
- 제목, 설명, CTA는 실제 텍스트로 작성하고 이미지에 포함하지 않는다.
- 자동 재생 캐러셀은 기본 5초 이상 간격, 일시정지 버튼, 이전·다음 버튼, 현재 슬라이드 정보를 제공한다.
- 모바일에서 좌우가 잘려 의미가 사라지지 않도록 별도 크롭을 제공한다.
- `prefers-reduced-motion` 사용자는 자동 전환과 큰 모션을 비활성화한다.

### 상품 카드 (SSG식 정보 위계 참고)

표시 순서는 다음과 같다.

1. 상품 이미지와 찜 버튼
2. 브랜드명
3. 상품명
4. 할인율, 판매가, 정상가
5. 배송·쿠폰·증정·신상품 배지
6. 평점과 후기 수

- 이미지 비율은 `1:1`을 기본으로 하고 `object-fit: cover`를 사용한다.
- 정상가는 취소선만 사용하지 말고 스크린 리더용 `정상가` 문구를 제공한다.
- 리뷰가 없으면 `0.0` 대신 `후기 없음`으로 표시한다.
- 품절 상품은 이미지 투명도만 낮추지 말고 품절 라벨과 비활성 상태를 함께 제공한다.

### 버튼·입력·상태

- 주요 버튼: 짙은 녹색 배경 + 흰색 텍스트
- 보조 버튼: 흰색 배경 + 짙은 텍스트 + 명확한 테두리
- 위험 버튼: 빨간색을 기본 행동과 분리하고 확인 문구 제공
- 포커스: `2px solid #005FCC`와 `2px` 오프셋을 유지
- 오류 메시지는 입력창 바로 아래에 해결 방법까지 표시하고 `aria-describedby`로 연결
- 로딩 중에는 중복 주문을 막고 진행 상태를 텍스트로 알림

## 6. 한국 사용자 현지화

- 문서 언어는 `<html lang="ko">`로 지정한다.
- 날짜는 `2026. 8. 29.` 또는 `8월 29일`처럼 한 화면 안에서 일관되게 사용한다.
- 금액은 `39,900원`, 할인은 `20%` 형식으로 표시한다.
- 전화번호와 주소 입력은 국내 형식을 기본으로 하되 하이픈 유무를 모두 허용한다.
- 주소 검색은 도로명주소를 기본으로 하고 지번주소와 상세주소를 보조한다.
- 이름은 2~4자로 제한하지 않는다. 한글 외 합법적인 이름 문자도 허용한다.
- 성과 이름을 불필요하게 분리하지 않으며, 존댓말과 간결한 행동형 문구를 사용한다.
- 결제·배송 단계에서 `오늘 도착`, `내일 도착 예정`처럼 기준 시각과 조건을 함께 표시한다.
- 법정 필수 동의와 선택 마케팅 동의를 분리하고 선택 항목을 미리 체크하지 않는다.

## 7. 이미지·폰트·색상 보안 및 권리 체크

### 이미지

- 참고 사이트의 로고, 배너, 상품 사진, 아이콘, 인물 사진을 다운로드하거나 핫링크하지 않는다.
- 직접 제작했거나 사용 권한이 확인된 이미지, 계약된 스톡 이미지, 브랜드가 제공한 승인 소재만 사용한다.
- 이미지 업로드는 허용 형식을 `JPEG`, `PNG`, `WebP`, 필요 시 `AVIF`로 제한하고 파일 확장자가 아닌 실제 MIME과 파일 시그니처를 검사한다.
- 업로드 이미지는 서버에서 재인코딩해 악성 메타데이터와 삽입 페이로드를 제거하고 EXIF 위치·기기 정보를 삭제한다.
- SVG는 신뢰된 내부 아이콘만 허용한다. 외부 SVG는 스크립트, 이벤트 핸들러, 외부 참조를 제거하는 검증 없이 직접 렌더링하지 않는다.
- 파일 크기와 해상도 상한을 두어 이미지 폭탄과 저장소 고갈을 방지한다.
- 사용자 업로드 파일은 실행 권한이 없는 별도 저장소와 별도 도메인에서 제공하고, 예측 불가능한 파일명을 사용한다.
- 이미지 CDN은 HTTPS, 접근 제어, 서명 URL, 캐시 정책을 사용한다.
- 모든 의미 있는 이미지에 한국어 대체 텍스트를 제공하고 장식 이미지는 빈 `alt`를 사용한다.

### 폰트

- 공식 배포처에서만 받아 무결성과 라이선스를 기록한다.
- 외부 폰트 CDN 의존을 피하고 WOFF2를 자체 호스팅한다.
- 필요한 굵기와 글자 집합만 배포해 개인정보 노출과 성능 저하를 줄인다.
- `font-display: swap`을 적용하고 폴백 폰트의 크기 차이로 레이아웃이 크게 흔들리지 않게 조정한다.
- CSS와 폰트 파일에는 사용자 입력 또는 임의의 원격 URL을 삽입하지 않는다.

### 색상

- 색상 값 자체는 보안 취약점이 아니지만, 사용자 입력 색상을 인라인 스타일로 그대로 삽입하면 CSS 인젝션 위험이 생긴다.
- 테마 값은 허용된 디자인 토큰 중 하나만 선택하도록 하고, 임의 CSS 문자열을 저장하거나 렌더링하지 않는다.
- 상태와 금액 정보를 색만으로 전달하지 않아 피싱성 강조와 접근성 문제를 줄인다.
- 결제·로그인 화면은 일반 프로모션과 시각적으로 구분하고, 신뢰 표시는 검증 가능한 사실만 사용한다.

### 웹 보안 기본값

- `Content-Security-Policy`로 스크립트, 이미지, 폰트 출처를 허용 목록 기반으로 제한한다.
- 모든 콘텐츠는 HTTPS로 제공하고 혼합 콘텐츠를 차단한다.
- 사용자 입력은 출력 위치에 맞춰 이스케이프하고, HTML을 허용해야 한다면 검증된 sanitizer를 사용한다.
- 인증 쿠키는 `Secure`, `HttpOnly`, `SameSite`를 적용한다.
- CSRF 방어, 로그인·쿠폰·주문 API의 속도 제한, 서버 측 가격 재검증을 적용한다.
- 상품 가격, 재고, 할인, 배송비는 클라이언트 값을 신뢰하지 않고 결제 직전 서버에서 다시 계산한다.
- 외부 링크에는 필요에 따라 `rel="noopener noreferrer"`를 적용한다.
- 분석 도구에는 검색어, 주소, 전화번호, 주문 정보 등 개인정보를 전송하지 않는다.

권장 CSP 예시는 프로젝트의 실제 도메인 목록에 맞춰 더 좁게 조정한다.

```http
Content-Security-Policy: default-src 'self'; script-src 'self' 'nonce-{RANDOM}'; style-src 'self'; img-src 'self' data: https://images.example-cdn.kr; font-src 'self'; connect-src 'self' https://api.example.kr; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests
```

## 8. 접근성 검수 기준

- WCAG 2.2 AA를 목표로 한다.
- 키보드만으로 검색, 카테고리, 캐러셀, 옵션 선택, 장바구니, 주문이 가능해야 한다.
- 논리적인 제목 구조와 랜드마크(`header`, `nav`, `main`, `footer`)를 사용한다.
- `바로가기` 링크를 첫 포커스 항목으로 제공한다.
- 아이콘 단독 버튼에는 한국어 접근성 이름을 제공한다.
- 모달이 열리면 포커스를 내부에 가두고 닫은 뒤 원래 위치로 되돌린다.
- 동적 가격·재고·장바구니 변화는 적절한 라이브 영역으로 알린다.
- 200% 확대와 모바일 가로 화면에서도 핵심 기능이 가려지지 않아야 한다.
- 실제 한국어 문장과 긴 상품명으로 줄바꿈을 테스트한다.

## 9. 성능 기준

- LCP `2.5초 이하`, INP `200ms 이하`, CLS `0.1 이하`를 목표로 한다.
- 첫 화면의 대표 이미지 한 장만 우선 로드하고 나머지는 지연 로드한다.
- 반응형 이미지에 `srcset`과 `sizes`를 사용하고 표시 크기에 맞는 파일을 전송한다.
- 이미지에 `width`와 `height` 또는 `aspect-ratio`를 지정해 레이아웃 이동을 막는다.
- 캐러셀과 추천 모듈을 초기 렌더링의 필수 조건으로 만들지 않는다.
- 서드파티 태그는 최소화하고 동의 전 광고·행동 추적 스크립트를 실행하지 않는다.

## 10. 출시 전 체크리스트

### 디자인·콘텐츠

- [ ] 참고 사이트의 로고, 카피, 배너, 상품 이미지, 아이콘을 복제하지 않았다.
- [ ] 모든 이미지와 폰트의 출처·라이선스·사용 범위를 기록했다.
- [ ] 한국어 문장, 원화, 날짜, 주소, 전화번호 형식을 실제 데이터로 검수했다.
- [ ] 모바일 360px부터 와이드 화면까지 레이아웃을 확인했다.
- [ ] 할인·품절·선택·오류가 색상 없이도 이해된다.

### 접근성

- [ ] 텍스트와 UI 요소의 명암비가 기준을 통과한다.
- [ ] 키보드 포커스가 항상 보이고 순서가 자연스럽다.
- [ ] 자동 전환 배너를 정지할 수 있다.
- [ ] 이미지 대체 텍스트와 입력 레이블이 제공된다.
- [ ] 200% 확대, 스크린 리더, 모션 감소 설정을 테스트했다.

### 보안·개인정보

- [ ] 이미지 MIME·시그니처·크기·해상도를 검사하고 재인코딩한다.
- [ ] 외부 SVG와 사용자 HTML을 안전하게 정제한다.
- [ ] CSP와 HTTPS, 보안 쿠키, CSRF 방어가 적용되었다.
- [ ] 가격, 할인, 재고, 배송비를 서버에서 재검증한다.
- [ ] 검색·분석·오류 로그에 개인정보가 남지 않는다.
- [ ] 필수 동의와 선택 동의가 구분되고 보관 기간이 명시된다.

## 11. 구현 시 피해야 할 것

- 참고 사이트와 혼동될 정도로 같은 로고 형태, 브랜드명, 색상 조합, 배너 아트 스타일 사용
- 이벤트 문구를 이미지에만 넣거나 작은 글씨로 조건을 숨기는 방식
- 밝은 강조색 위 흰색의 작은 텍스트
- 자동 재생되며 멈출 수 없는 캐러셀과 과도한 팝업
- 클라이언트에만 존재하는 할인·재고·결제 검증
- 외부 이미지 URL, SVG, CSS, 폰트 URL을 사용자 입력대로 렌더링하는 기능
- 키보드 포커스를 제거하는 CSS와 클릭 가능한 `div`

---

이 문서는 SSG.COM에서 확인한 구조적 장점만 추출한 초기 디자인 기준이다. SSG.COM의 로고·브랜드명·상품 이미지·문구·고유 색상은 사용하지 않는다. 실제 구현에서는 SHOP.md의 기능과 데이터가 우선하며, 데이터가 없는 검색·프로모션·배송·적립·리뷰 요소는 만들지 않는다.
